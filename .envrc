#!/bin/sh

case "${IN_NIX_SHELL:-}" in
"")
  DOTS="$(git rev-parse --show-toplevel 2>/dev/null || pwd -P)"
  DOTS_INIT_COMMAND="load-flake"
  export DOTS DOTS_INIT_COMMAND
  . "$DOTS/profile"
  ;;
*) ;;
esac
