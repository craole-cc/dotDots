{
  colors = {
    alpha = 0.97; # Slight transparency (1.0 = opaque)
  };

  cursor = {
    style = "beam"; # Options: block, underline, beam
    blink = "yes";
    blink-rate = 500;
    beam-thickness = 1.5;
  };
}
