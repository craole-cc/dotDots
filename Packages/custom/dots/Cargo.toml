# Packages/custom/dots/Cargo.toml
[package]
name = "dots-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive", "cargo"] }
anyhow = "1.0"
serde_json = "1.0"
colored = "2.0"
arboard = "3.2"

[profile.release]
opt-level = 3
lto = true
