# File selection options
[files]
extend-exclude = ["vendor/**", "**/*.min.js", "**/*.lock", ".typos.toml"]
ignore-hidden = true
ignore-files = true

# Default engine behavior
[default]
binary = false
check-filename = true
check-file = true
unicode = true
locale = "en-us"

# Custom dictionary entries
[default.extend-identifiers]
myidentifier = "myidentifier" # Always valid
mispeled = "misspelled"       # Will be corrected
"colour" = "color"
"nitch" = "nitch"

[default.extend-words]
# "nitch" = "nitch"
"optimise" = "optimise"
"dots-fetch" = "dots-fetch"


# Custom ignore patterns
# [default.extend-ignore-re]
# - "(?s)```.*?```"
# - "`[^`]+`"
# Ignore code blocks in markdown
# Ignore inline code in markdown

# # Type-specific configurations
# [type.js]
# # JavaScript-specific settings
# [type.js.extend-identifiers]
# getElementsByClassName = "getElementsByClassName"
